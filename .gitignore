# Configuración local de Claude Code (permisos y skills de esta máquina,
# no es parte del producto)
.claude/

# Sistema operativo / editores
.DS_Store
Thumbs.db
*.swp

# impeccable (hook de diseño): la config compartida sí se versiona; el resto no
.impeccable/hook.cache.json
.impeccable/hook.pending.json
.impeccable/config.local.json
